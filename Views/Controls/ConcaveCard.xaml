<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:AkilliEvMobil.Views.Controls"
             x:Class="AkilliEvMobil.Views.Controls.ConcaveCard">
    <Grid>
        <!-- Arka planı çizen GraphicsView -->
        <GraphicsView x:Name="CardGraphicsView" />
        
        <!-- İçeriğin ekleneceği kapsayıcı -->
        <ContentView x:Name="CardContent" />
    </Grid>
</ContentView>
